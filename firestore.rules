rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full access for development
    // ⚠️ WARNING: Change these rules for production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
